### $HOME/.config

Not everything in $HOME/config is linked to here, because there are some modern apps that 
just store huge gigabytes of database, cache, etc in $HOME/.config/<app> so I only keep
a few useful CLI tool configs here in the dotfiles. 



